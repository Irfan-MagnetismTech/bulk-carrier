import { computed, onMounted, ref } from 'vue';
import { useStore } from "vuex";
/**
 * @typedef {Object} FileDownload
 * @property {function} downloadFile
 */
export default function tble scrollable
    return {
        downloadFile
    }

}


  


